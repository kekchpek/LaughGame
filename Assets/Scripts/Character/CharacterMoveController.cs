using System;
using UnityEngine;

namespace LaughGame.Character
{
    public class CharacterMoveController : MonoBehaviour
    {
        public void Update()
        {
            if (Input.GetKey(KeyCode.W))
            {
                
            }
        }
    }
}